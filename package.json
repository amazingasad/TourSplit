{
  "name": "toursplit-pro",
  "version": "1.0.0",
  "description": "TourSplit Pro - Group Expense Tracker",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step needed - static web app'",
    "cap:sync": "npx cap sync",
    "cap:build:android": "npx cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/filesystem": "^6.0.0",
    "@capacitor/share": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
